version: "3.6"

services:
  thisrepo-testing:
    container_name: "${BUILD_TAG}"
    working_dir: /srv
    build:
      context: ./.sqa
    hostname: "${BUILD_TAG}"
    command: >
      sh -c "git config --global --add safe.directory /srv &&
             tail -f /dev/null"
    volumes:
      - type: bind
        source: ./
        target: /srv
